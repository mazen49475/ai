# =============================================================================
# Trash Bin Detection - Python Dependencies
# Optimized for Raspberry Pi 4 64-bit
# =============================================================================

# Core ML/CV (YOLOv8)
ultralytics>=8.0.0
opencv-python-headless>=4.8.0
numpy>=1.24.0

# Raspberry Pi Camera (Picamera2 for 64-bit OS)
picamera2>=0.3.12

# Web Interface
flask>=3.0.0
flask-socketio>=5.3.0
gevent>=23.9.0
gevent-websocket>=0.10.1

# Utilities
pyyaml>=6.0
Pillow>=10.0.0
requests>=2.31.0

# Logging & Monitoring
psutil>=5.9.0

# GPIO for buzzer alerts on Raspberry Pi
# Note: RPi.GPIO is pre-installed on Raspberry Pi OS
# Install manually if needed: pip install RPi.GPIO
RPi.GPIO>=0.7.1

# PyTorch (CPU version for Raspberry Pi - installed automatically with ultralytics)
# torch>=2.0.0
# torchvision>=0.15.0
